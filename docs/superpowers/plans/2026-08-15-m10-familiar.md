# M10 — Familiar Implementation Plan (v1.2: real families, named neighbors)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Answer the v1.1 user review — no creature ever freezes or crosses water; feeding and births play out true to each species (nursing mothers, shell-crack hatches, burrow emergences, self-sufficient fry); labels give way to animated cues; any creature can be tapped to meet ("Willow · mother of the Bramble family"); animation gains crossfade smoothness; and three new neighbors move in: squirrel, frog, turtle.

**Architecture:** Three sim tasks lead (bug fixes → feedMode split → new species), each with M9-style threshold discipline (re-baselines only with per-change root-cause justification; determinism replay + persist inviolable; WorldState gains NO new fields — saves stay v2, species/rig additions are data). Render/UI tasks follow on the stable sim: birth/hatch staging + animated cues, tap-to-inspect card, crossfade polish. Model tiering per task is annotated (cost discipline is a user requirement).

**Tech Stack:** PixiJS v8, existing sim/render/rigs/audio/ui, Vitest, ffmpeg + Wikimedia Commons CC audio (per LICENSES.md gate).

**Spec:** docs/superpowers/reviews/2026-08-15-v1.1-user-review.md (feedback, root causes with file:line, decisions) + docs/superpowers/specs/2026-08-13-beastoria-v1-design.md (§gentle realism binds).

## Global Constraints

- `src/sim/` purity as ever; creatures iterated in array order; WorldState/Creature/Family gain NO new persisted fields (new species/home kinds are data values inside existing fields — verify a v2 save without them loads: unknown-species guard not needed since old saves simply lack them). Determinism replay + persist tests pass UNCHANGED in every task. Seeded thresholds may move only in Tasks 1-3, each with a justification table row.
- Audio: CC0/PD/CC-BY/CC-BY-SA only, per-file provenance rows in public/audio/LICENSES.md; never xeno-canto/BBC. Turtle ships silent (realistic) — no filler audio.
- Render rules: no live filters; textures baked once; no per-frame allocation; gentle aesthetic (nothing flashes/fast; crack/emerge/crossfade all eased, ≥ 600ms).
- New species = data + rig files following existing exemplars — never new systems. New home kinds are species-data + one draw case in syncHomes.
- Testing policy: sim + persist tests only; render/UI eyeballed honestly.
- Commits end with trailer `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`.
- Model tiers annotated per task: [impl: X / review: Y]. Re-reviews take haiku unless the fix diff is sim-behavioral (then sonnet).

## Key existing interfaces

- movement.ts: `moveToward(c, target, speed, medium, landing)` returns remaining distance, snap-arrival at :28-34 (returns 0 WITHOUT moving if target illegal — the bug), `advance` normal branch validates candidates (:85-98), stranded-escape branch does NOT (:69-79); `nearestRestable(medium, p)` in state.ts.
- behaviors.ts: socialize/court ring at :207-211 (unvalidated); `forageTarget`/`nearestFoodSpots` (:385-397, phoenix-only special case); FOOD_SPOTS (19; 4 reed spots zone 'pond') in valley.ts; STOPPED_ACTIVITIES/restingIsLegal machinery; feedYoung at :233-270 (step 0 fetch random point, step 1 carry to home.pos, −0.35 hunger per baby on arrival, minTicks 30).
- family.ts: feed trigger :329-341 (FEED_TRIGGER_HUNGER 0.5, dutyParent rotates); nesting→expecting rolls clutch, 'eggLaid' emitted for egg mode (:222-232); expecting: live mode pins sitter to mother `parents.find(p=>p.sex==='f')` (:247-259); hatch/birth: spawnCreature at home ± (±25,±18), event 'hatched'|'born' pos=home.pos (:266-280); childIds append in birth order.
- species.ts: `SpeciesParams` with `reproduction.mode: 'egg'|'live'`, medium/landingMedium, speed, caps, zones, homes. 8 species. `landingMediumOf()`.
- Renderer: debugLabels=true default (:248, label gate :758); glyph layer + glyphKindFor (:1080); SPARKLE_EVENT_KINDS + consumeNewEvents watermark (:160, :439); syncHomes full-redraw egg draws per home kind (:450-527); pickCreature(state, clientX, clientY) (:345-358); familyName(fam.id) (:1089); clipFor (:1042-1060); fading list; applyStage.
- DevPanel: tap-vs-drag (>6px) on canvas (:79-91) → pickCreature → selectedId + followId.
- Animator: play()/applyClipVisibility/update(ms) with per-part base + track sampling; no crossfade (pose snaps on play()).
- audio: CallScheduler consumes sim 'vocalize' events; species voice defs in sim (voice.ts) + clips under public/audio/families/<species>/; M6 ffmpeg pipeline documented in LICENSES.md rows.
- Hud: PILL_CSS pills top-left clock, top-right sound + fullscreen.

---

### Task 1: No creature freezes, none walks on water (sim bugs) [impl: sonnet / review: opus]

**Files:** Modify src/sim/behaviors.ts, src/sim/movement.ts, src/sim/valley.ts · Tests: new tests/stuck.test.ts, modify tests/foodspots.test.ts (+ justified seeded updates)

**Interfaces:** Produces `moveToward` returning `-1` on failed snap (target illegal) instead of a lying 0 — all call sites audited to treat <0 as "not arrived" (most ignore the return; socialize/court must now fall back); socialize/court ring target clamped `nearestRestable(landing, ring)`; a progress bail: socialize/court approach that has not moved ≥ 1 world unit in 100 consecutive ticks drops to `startActivity('wander')` (deterministic tick counter on the activity's existing `ticks` field arithmetic — NO new Creature fields; recompute via stored `targetPos` distance instead if a counter would need state — implementer picks the stateless variant and documents it); stranded-escape branch validates each candidate step with `canOccupy` mirroring the normal branch (turn-and-retry; if all candidates illegal, hold position — never a blind step); `nearestFoodSpots` filters reed-zone spots out for species whose `landingMedium ?? medium === 'land'` (rabbits/deer/squirrel never forage the shore band; amphibious/water/air species unchanged).

- [ ] **Step 1: Failing tests.** tests/stuck.test.ts: (a) rabbit at the pond's west shore socializing a partner whose ring point lands in water — run 300 ticks — assert the rabbit either reaches social relief or changes activity, and its position changes by > 5 units within any 150-tick window (fails today: motionless forever); (b) plant a rabbit at an illegal in-pond position, run advance-driven ticks, assert every intermediate position satisfies `!isWater` OR is strictly closer to `nearestRestable` than the prior tick with `isWater` false within 40 ticks and NO tick moving deeper into the pond (fails today: unchecked full steps); (c) foodspots: for 500 seeded forage target picks per land species, assert no target within the pond ×1.18 shore band.
- [ ] **Step 2:** Watch each fail for the right reason. **Step 3:** Implement per Interfaces. **Step 4:** Full suite; justified-threshold table for any seeded movement (reed-spot removal changes land-species pick sets — expected); determinism/persist untouched. Lint (sim purity), build. **Step 5:** Commit — `fix: nobody freezes mid-hello, nobody walks on the pond`

---

### Task 2: Feeding the way each species really does (sim) [impl: sonnet / review: opus]

**Files:** Modify src/sim/species.ts, src/sim/family.ts, src/sim/behaviors.ts, src/sim/state.ts (activity union only if needed — no persisted shape change; 'nurse' rides the existing activity.id string field) · Tests: new tests/feeding.test.ts (+ justified seeded updates)

**Interfaces:** Produces `reproduction.feedMode: 'nurse' | 'carry' | 'self'` on SpeciesParams — rabbit/deer 'nurse', robin/duck/owl/dodo/phoenix 'carry', koi 'self' (squirrel/frog/turtle arrive in Task 3 with nurse/self/self). Behavior per mode: **carry** = exactly today's flow, untouched. **nurse** = trigger unchanged (family.ts:329-341) but the feeder is ALWAYS the mother (sex 'f' fallback parents[0]); activity 'feedYoung' step 0 = moveToward home.pos (no fetch trek); on arrival step 1 = stationary nursing hold, minTicks 80; during step 1 every family baby within 60 units has hunger reduced 0.006/tick (≈0.48 over the hold — same draw count as today: zero draws in the hold loop); baby-leash already pulls babies home so they gather to her. **self** = feed trigger skipped entirely for the species (family.ts guard) AND babies of self species get passive grazing: while stage==='baby', hunger additionally decays 0.0015/tick when resting is legal (filter-feeding fry/tadpoles/hatchlings) — deterministic arithmetic, no draws, keeps them alive without parents (verify against balance suite).

- [ ] **Step 1: Failing tests** — tests/feeding.test.ts: (a) rabbit family with hungry babies: the MOTHER (sex f) ends up at home in a stationary step-1 hold and babies' hunger drops while she holds (assert her position unchanged across the hold and hunger monotonically decreasing); (b) robin family: flow identical to pre-change (fetch point away from home, then carry home — assert step 0 target ≠ home and step 1 target = home); (c) koi family with hungry fry: NO parent ever enters feedYoung over 2000 ticks AND fry hunger stays < 0.9 (passive grazing sustains them).
- [ ] **Step 2:** Fail right. **Step 3:** Implement. **Step 4:** Full suite + justification table (feedYoung draw-pattern changes for nurse species are expected and confined here); determinism/persist untouched; lint/build. **Step 5:** Commit — `feat: mothers nurse, birds ferry, fry fend — feeding grows true`

---

### Task 3: Three new neighbors — squirrel, frog, turtle (sim data + rigs + voices) [impl: sonnet / review: opus]

**Files:** Create src/rigs/squirrelRig.ts, frogRig.ts, turtleRig.ts; audio under public/audio/families/{squirrel,frog}/ · Modify src/sim/species.ts, src/sim/valley.ts (home sites), src/sim/voice.ts, src/render/Renderer.ts (syncHomes draw cases + RIGS map), src/rigs/format.ts (only if a shared type needs widening), public/audio/LICENSES.md · Tests: extend tests/balance.test.ts property suite + tests/foodspots.test.ts if spots added, new species rows in tests/rigs.test.ts

**Interfaces:** Produces three SpeciesParams following existing exemplars exactly: **squirrel** — quadruped, medium 'land', speed 7, strideLength 20, forest zone, reproduction { mode:'live', feedMode:'nurse' }, clutch 2-3, softCap 6, hardCap 10, home kind 'drey' (new: twiggy ball high in a forest tree — one syncHomes draw case, site list in valley.ts near FOREST like burrows); **frog** — new squat archetype (rig from rabbit's structure: round body, big eyes, fold-legs; hop gait = body py bob 8 with legs-tuck like robin's hop, strideLength 14), medium 'amphibious', landingMedium undefined, speed 4, pond zone, { mode:'egg', feedMode:'self' }, clutch 4-6, softCap 8, hardCap 14, home 'spawnClump' (jelly dots among reeds — draw case near lilyPatch sites); **turtle** — low wide archetype (domed shell part, head, four stub legs, tail nub; walk = slow leg paddles, strideLength 10), medium 'amphibious', speed 2 (the valley's slowest — serene), shore/pond zone, { mode:'egg', feedMode:'self' }, clutch 2-4, softCap 5, hardCap 8, home 'sandNest' (small sand mound on the shore band — draw case). All three: full clip set (idle/walk/sleep/eat/social/carry/sit + swim for frog/turtle per Task 4-era machinery already in format), stages via parametric overrides, food part with exhaustive hideInClips. Voices: frog croak + squirrel chatter sourced from Wikimedia Commons CC (M6 ffmpeg pipeline: trim/highpass/denoise/loudnorm → webm+m4a, LICENSES.md rows with URL/author/license/edits); turtle silent (voice def with no clips / never vocalizes — follow how voice.ts gates species). Population: wanderer arrivals include all three; balance property suite extended with their bands (6 seeds × 30 days + soak stays green — tune caps/fertility as needed WITH justification rows).

- [ ] **Step 1:** Species + rig data (compile-enforced clip completeness via CoreClipName Record), home sites/draw cases, voices + LICENSES.md rows. **Step 2:** Balance property extension: per-species population band asserts (>0 after day 5, ≤ hardCap every tick) across the seed matrix; run/tune/justify. **Step 3:** Full suite, lint, build; eyeball: three species visible, moving right (squirrel darts-and-pauses: idle minTicks bias is data — give squirrel idle minTicks 10-30 for dartiness; turtle glacial; frog hops), audio credited and audible for frog/squirrel. **Step 4:** Commit — `feat: three new neighbors — a darting squirrel, a patient turtle, a chorus of frogs`

---

### Task 4: Births you can witness, cues without words (render) [impl: sonnet / review: sonnet]

**Files:** Modify src/render/Renderer.ts, src/render/effects/Ambient.ts, src/render/creatures/Animator.ts (only if a nurse pose needs it), rig files (nurse pose reuse 'sit'; no new clips expected)

**Interfaces:** Consumes Task 2's nurse flow + 'eggLaid'/'hatched'/'born' events (exactly-once watermark exists). Produces: **hatch staging** — on 'hatched' at an egg home: shell-crack overlay at the nest (baked 3-state shell texture: whole→cracked→halves, ~700ms per state, then halves fade 800ms) while each new baby view scales 0→1 eased 900ms from the nest point (suppress the generic sparkle for hatches — the crack IS the moment; keep sparkle for 'paired'/'reborn'); **birth staging** — on 'born' at burrow/glade/drey: baby views emerge — start at the home mouth, scale 0.3→1 + slide 12px outward over 900ms; **egg continuity** — eggs stop vanishing abruptly: when phase flips from expecting, syncHomes' egg draw is replaced by the crack overlay for egg modes (live modes just stop drawing — nothing was visible); **nursing read** — mother in step-1 nurse hold gets 'sit' clip + babies within 60 units get 'eat' clip snuggled (clipFor additions), glyph: soft milk-white droplet for nursing (replaces amber carry dot for nurse species); **labels off** — debugLabels default false (DevPanel toggle keeps working); **z·z·z** — sleeping creatures emit a tiny drifting 'z' every ~2.5s (baked 6px 'z' texture, rises 14px fading over 2s, pooled like sparkles, max 12 live); **glyph life** — glyphs gain a gentle 2.4s sine bob (±1.5px) and scale-in on appear (already alpha-eased); **duck shore hysteresis** — swimming state requires 3 consecutive frames of isWater agreement before toggling (kills shoreline clip flicker, M9 deferred minor).
- [ ] **Step 1:** Implement; per-view emergence/crack state as floats on existing view records; all textures baked once; pools bounded. **Step 2:** Gates: suite untouched from Task 3 baseline, lint, build; eyeball at 8x: a hatch cracking open, a burrow emergence, a nursing huddle, z·z·z over a sleeper, no text anywhere by default, no duck flicker at the shore. **Step 3:** Commit — `feat: shells crack, kits emerge, mothers nurse, sleepers drift z's`

---

### Task 5: Meet the neighbors — tap to inspect (ui/render) [impl: sonnet / review: sonnet]

**Files:** Create src/ui/InspectCard.ts · Modify src/main.ts, src/render/Renderer.ts (selection ring), src/app/DevPanel.ts (dedupe tap logic — main owns the tap now, DevPanel consumes the same selection)

**Interfaces:** Produces tap-to-inspect available ALWAYS (not just DevPanel): tap-vs-drag (>6px, reuse DevPanel's discriminator — move it to main.ts or a small shared helper) → `renderer.pickCreature` → InspectCard (PILL_CSS-styled panel, bottom-center, mobile-safe) showing: **name** — deterministic per-creature from a 24-entry gentle nature-name list via id-hash (same Math.imul pattern as idOffsetAngle; two creatures may share a name across the valley, never within a family — pick list index (hash + position-in-family) mod), **role** — "mother/father of the <familyName> family" (sex-based), "kid N" by childIds birth order, "elder", or "a wanderer (no family yet)", **species + stage**, **doing** — activity in plain words (map: forage→"looking for something tasty", nap→"dozing", feedYoung nurse→"nursing the little ones", carry→"bringing food home", brood→"keeping the eggs warm", court→"smitten", gather(mourn)→"paying respects", pass→"passing gently", wander/idle→"taking the air", flap-airborne→"on the wing", swim→"paddling about"). Tap selects + soft follow (existing followId); a subtle selection ring (baked circle, alpha 0.35) under the creature; tap empty ground or the card's × dismisses both card and follow. DevPanel's inspector keeps working off the same selectedId (single source: renderer or a small selection object in main).
- [ ] **Step 1:** Implement. **Step 2:** Gates: suite untouched, lint, build; eyeball: tap a rabbit mid-valley → card with name/role/doing; tap a kid → "kid 2"; tap ground → dismissed; drag still pans; pinch untouched; DevPanel tap still works. **Step 3:** Commit — `feat: tap a neighbor to meet them`

---

### Task 6: Crossfade smoothness (render) [impl: sonnet / review: sonnet]

**Files:** Modify src/render/creatures/Animator.ts, src/render/Renderer.ts

**Interfaces:** Produces pose crossfading: `play(name)` no longer snaps — Animator keeps the outgoing pose's last sampled values and lerps each part's transform from them to the incoming clip over 220ms (eased); part visibility (hideInClips) switches at the 50% point of the fade so props don't pop; bakes are unaffected (bakedFrame's fresh instance starts converged — verify). Arrival settle: when a view's clip changes walk→idle/eat, apply one gentle 300ms scale-y dip (0.97) on body — the "settle" the M8 investigation suggested. Guard: crossfade state is per-view floats; no allocation per frame; passing fade and emergence scale-in compose with crossfade without fighting (order: emergence/passing multiply node alpha/scale; crossfade only touches part transforms).
- [ ] **Step 1:** Implement + verify bake path convergence (bakedFrame must sample the TARGET clip exactly — force-complete the fade for fresh instances). **Step 2:** Gates: suite untouched, lint, build; eyeball at T2: clip switches melt into each other (walk→eat, walk→flap takeoff, sit→walk), no prop pops, no T1 regression. **Step 3:** Commit — `polish: poses melt, arrivals settle — animation breathes`

---

### Task 7: v1.2 close-out [impl: haiku / review: none (final review covers)]

- [ ] **Step 1:** Full verification: `npm test`, `npm run lint`, `npm run build` — clean.
- [ ] **Step 2:** CLAUDE.md: status → v1.2 (M10) shipped with compact content list (bugs fixed, feedMode realism, 3 new species = 11 total, birth/hatch staging, tap-to-inspect, crossfade, labels off); "Awaiting user review": live check on their devices (new species, inspect card, hatch moments, perf with 11 species). Working-process line M0–M9 → M0–M10.
- [ ] **Step 3:** Commit `docs: v1.2 — the valley grows familiar` (merge to main at finish; push deploys).

## Self-review notes

- All nine feedback items map: 1→T2/T4, 2→T4, 3→T1, 4→T1, 5→T4, 6→T5, 7→T6, 8→T3, 9→duck hysteresis (T4), reed-spot filter (T1), M9 deferred minors triaged at final review. Not doing: predators, v2 caretaker features, sim-side flight altitude.
- Re-baseline discipline confined to T1-T3 (each with justification tables); determinism/persist inviolable everywhere; no WorldState shape changes anywhere (nurse rides existing activity fields; species are data).
- Perf risk: 11 species raise population ceiling — balance caps chosen conservatively (squirrel 10, frog 14, turtle 8 hard); T3's balance extension + the user's device pass gate it; glyph/z pools bounded.
- Cost plan honored: opus only on the three sim reviews + final review; haiku close-out; sonnet everywhere else; controller (Fable) never implements.
